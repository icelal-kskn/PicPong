#ifndef SPI_H
#define	SPI_H
//------------------------------------------------------------------------------
#include "Soft_SPI_Pins.h"
//------------------------------------------------------------------------------
void SPI_init(void);
//--------------------------TX: transmit data-----------------------------------
void SPI_write(char send);
//--------------------------RX: recieve data------------------------------------
char SPI_read(void);
//------------------------------------------------------------------------------
#endif